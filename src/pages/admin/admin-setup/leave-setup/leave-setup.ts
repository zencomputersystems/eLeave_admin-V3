import { Component, OnInit } from '@angular/core';

/**
 * Leave header with router-outlet page 
 * @export
 * @class LeaveSetup
 * @implements {OnInit}
 */
@Component({
    selector: 'app-leave-setup',
    templateUrl: './leave-setup.html',
    styleUrls: ['./leave-setup.scss'],
})
export class LeaveSetup implements OnInit {

    /**
     *Creates an instance of LeaveSetup.
     * @memberof LeaveSetup
     */
    constructor() { }

    ngOnInit() {
    }

}